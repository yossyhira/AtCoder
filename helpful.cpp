//順列全探索
#include <bits/stdc++.h>
using namespace std;

int main() {

    vector<int> p(9);//9!の全探索
    rep(i, 9) p[i] = i;

    int cnt = 0, tot = 0;
    do {
        
    } while (next_permutation(p.begin(), p.end()));
    return 0;
}
