#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int a;
    cin >> a;
    cout << 4*a*a << endl;
    cout << 4*ll(a)*ll(a) << endl;
}