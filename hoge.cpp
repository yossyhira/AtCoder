#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout << 100 << " " << 100 << endl;
    for(int i = 0; i < 100; i ++){
      for(int j = 0; j < 100; j ++){
        cout << '.';
      }
      cout << endl;
    }
    return 0;
}